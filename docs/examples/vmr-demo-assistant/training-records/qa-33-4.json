{"question": "What happens when an unknown key signs a record?", "answer": "An impostor can sign with its own key, but the verifier's trust store does not trust that key for the issuer it names, so the record is not valid."}
