{"question": "Why does my record fail every policy pack?", "answer": "The packs only check what a record declares. A record that pins its training environment, commits its training records, states its data collection period and pins its documents can pass; one that declares none of these cannot."}
